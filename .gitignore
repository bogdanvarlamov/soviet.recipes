# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual Environments
.venv/
venv/
ENV/
env/

# uv
uv.lock

# pytest
.pytest_cache/
.coverage
htmlcov/
.tox/
*.cover
.hypothesis/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log

# Environment variables
.env
.env.local

# Output directories (for processed images/text)
output/
outputs/
processed/
experiments/
page-splits/

# Temporary files
*.tmp
*.bak

# Generated debug/visualization images (Docling OCR debug + markdown renders).
# These are large (~32 MB each, ~39 GB total) and fully regenerable from the
# pipeline, so they're kept out of git to avoid bloating the repo and blocking
# pushes to GitHub. The lightweight text results (.txt/.md/.json) are still tracked.
phase_2/processing_results/**/*.png
